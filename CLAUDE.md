# 협업 Git 동기화 규칙

이 프로젝트는 사용자와 회사 직원이 동시에 작업합니다. 사용자가 "동기화해줘" 또는
"sync"라고 요청하면 다음 순서를 자동으로 진행합니다:

1. `git status`로 현재 로컬 변경사항 확인
2. 변경된 파일이 있으면 의미 있는 커밋 메시지로 `git add` + `git commit`
   (파일 지정해서 add, `-A`로 통째로 add하지 않음)
3. `git pull` — 직원이 그 사이 push한 변경사항을 받아옴
   - 자동 병합되면 계속 진행
   - 충돌(conflict)이 나면 자동으로 풀지 말고, 충돌 파일/내용을 보여주고 사용자에게 물어봄
4. push 직전에 반드시 멈춰서 **커밋 요약(무엇을 바꿨는지) + 커밋 메시지**를 보여주고
   사용자의 확인을 받은 뒤에만 `git push` 진행 (자동으로 바로 push하지 않음)

`git status`만 단독으로 입력했을 때는 위 동기화 절차를 실행하지 않고, 평소처럼
순수 상태 조회만 한다. 동기화를 원하면 "동기화해줘"/"sync"라고 명시적으로 말해야 한다.
